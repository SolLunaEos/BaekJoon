#include<stdio.h>
int main()
{
	int a,x,i,j,num[10000];
	scanf("%d %d",&a,&x);
		
	for(i=0; i<a; i++)
	{
		scanf("%d",&num[i]);
	}
	
	for(i=0; i<a; i++)
	{
		if(num[i]<x)
		{
			printf("%d ",num[i]);
		}
	}
	
}
